import TransitHomeClient from '@/components/transit/TransitHomeClient';

export const metadata = {
  title: 'Transit, Logistics & Transport | EL-BOMI GROUP',
  description: 'Transit douanier, transport multimodal, entreposage et distribution en Afrique de l\'Ouest — EL-BOMI Transit.',
};

export default function TransitPage() {
  return <TransitHomeClient />;
}
